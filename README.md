# nvim-translate-byte-literal

This neovim plugin use nix flake to run a RPC call, which can transfer the byte array to string.

- Command to transfer byte array in current line
  :TranslateByteArray
